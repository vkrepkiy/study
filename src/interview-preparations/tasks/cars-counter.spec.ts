/**
 *
 * @param arr
 * 1 <= arr.length <= 100000
 * 0 <= arr[i] <= 1
 */
function countCars(arr: number[]): number {
  let pairs = 0;

  // meet the task conditions
  return pairs > 1000000000 ? -1 : pairs;
}

describe("countCars", () => {
  it("should return 5", () => {
    expect(true).toBe(true);
  });
});
